# theme-switcher
Script for switching neovim/thmux/awesome-wm/zsh/kitty themes using dmenu

# Dependencies
* nvim v.0.8 (requires nvim's remove client-server feature)
* dmenu

# Usage
Run `theme_prompt.bash` and select the theme using dmenu.
